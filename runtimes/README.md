# Agent runtimes

Reserved for replaceable adapters that manage model conversation mechanics.
Runtime implementations do not own domain data, permissions, or durable work state.

Local CLI and hosted API/SDK operation are candidate approaches. No provider,
runtime implementation, credentials, or subscription access ships in this directory.
