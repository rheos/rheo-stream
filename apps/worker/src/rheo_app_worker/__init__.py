"""Worker entry-point stub.

The composition root apps/worker fills in 0c: it polls each active workspace's
core.job / core.event_delivery tables and shares the core image. No callable yet —
an entry-point stub, not a runnable worker.
"""
